# private planning docs — scrub pass required before these ever go public
docs/

# os / editor
.DS_Store
*.swp

# deps / build
node_modules/
dist/
.astro/

# vercel
.vercel

# env
.env
.env.*

# monorepo build / test artifacts (§1.9)
*.tsbuildinfo
coverage/
*.log
# unscrubbed harvest staging — raw blobs from a real chat.db must NEVER be committed (Part 3)
fixtures/typedstream/staging/
# local runtime artifacts if a dev runs the daemon from the repo
.wemessage-dev/

# s7 Sc7: Python bytecode. The plugin is run as a child process by the
# conformance rows, so an interpreter WILL write these next to the source;
# they are build output, they are binary, and they must not reach a public
# repository.
__pycache__/
*.py[cod]
.venv/
