// @wemessage/core/audit — audit-event vocabulary + hash-chain math (S2
// Scenario 4). Pure per INV-1; concept home per §3.12 (event construction +
// chain math only here; the append-only writer lives in store).
export * from './chain.js';
export * from './events.js';
